name: X264Encode
description: Program profile
scaling: bicubic
encodes:
  - type: X264Encode
    suffix: _x264_
    twoPass: true
    height:
    width:
    params:
      b:v:
      maxrate:
      minrate:
      r: 25
      fps_mode: cfr
      pix_fmt: yuv420p
      force_key_frames: expr:not(mod(n,96))